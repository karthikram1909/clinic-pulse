[build]
  base = "project"
  command = "npm run build"
  publish = "dist"     # << FIXED

[build.environment]
  # VITE_API_BASE will be set in Netlify UI

[dev]
  command = "npm run dev"
  port = 5173
