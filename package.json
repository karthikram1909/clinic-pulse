{
  "name": "clinic-pulse",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "cd project && npm run dev",
    "build": "cd project && npm run build",
    "install-all": "npm install && cd project && npm install && cd ../backend && npm install"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.0.2",
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^5.2.1",
    "mongoose": "^9.0.0",
    "pg": "^8.16.3",
    "serverless-http": "^4.0.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.11"
  }
}
